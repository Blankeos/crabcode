# Defaults

This folder contains copy/paste templates for Crabcode.

Config:

- `defaults/crabcode.jsonc` (recommended starting point)

Common install locations:

- Global: `~/.config/crabcode/crabcode.jsonc`
- Project: `<repo>/.crabcode/crabcode.jsonc`
