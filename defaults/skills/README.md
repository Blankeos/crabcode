# Default Skills (Planned)

This folder is reserved for future, built-in skill templates.

Phase 1 only discovers OpenCode skills folders (it does not load/apply skills yet).

Relevant doc:

- `_docs/config.mdx`
